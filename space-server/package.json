{
  "name": "space-server",
  "version": "1.0.0",
  "description": "WebSocket server for running space-clone-game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "npm run build && node dist/src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.4.1",
    "space-parks-engine": "file:../space-parks-engine/dist",
    "typescript": "^4.5.4"
  }
}
