{
  "name": "space-server",
  "version": "1.0.0",
  "description": "WebSocket server for running space-clone-game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "npm run build && node dist/src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.5.4",
    "ws": "^8.4.2"
  }
}
